package com.example.model

/**
 * Layout presentation mode for track collections across SoundSync library screens.
 */
enum class TrackLayoutMode(val displayName: String) {
    LIST("List View"),
    GRID("Grid View")
}
